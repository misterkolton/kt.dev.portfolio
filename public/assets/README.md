Asset folders:

- `icons/` for favicon and small UI icons
- `brand/` for logos and brand artwork
- `images/` for larger page imagery

Favicon path currently used by the app:

- `/assets/icons/favicon.svg`

If you drop in a PNG instead, use:

- `/assets/icons/favicon.png`

Then update `index.html` icon link type/href accordingly.
